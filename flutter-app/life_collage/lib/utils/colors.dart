import 'package:flutter/material.dart';

class AppColors {
  // Define all colors as static final members for easy access throughout the app.

  // Primary Theme Colors (Pastel & Warm)

  /// Hex: #F7F4EB - Used for Scaffold/Backgrounds
  static const Color creamyBase = Color(0xFFF7F4EB);

  /// Hex: #E8DCD5 - Used for Card/Surface containers
  static const Color softBeige = Color(0xFFE8DCD5);

  /// Hex: #DDA0AE - Used for Primary buttons, FABs, and active accents
  static const Color dustyRose = Color(0xFFDDA0AE);

  /// Hex: #FFC0A0 - Used for highlights, ratings, and secondary accents
  static const Color mutedPeach = Color(0xFFFFC0A0);

  // Text & High Contrast Colors

  /// Hex: #5C4033 - Used for text and icons (Warm Contrast)
  static const Color warmBrown = Color(0xFF5C4033);

  // Standard UI Colors (often needed)
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
